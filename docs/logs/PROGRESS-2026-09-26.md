# Progress — 26 September 2026

Policy previews now preserve numbering, bullets, headings and paragraph spacing in Admin and in Buyer/Seller notification dialogs. A shared renderer limits output to supported formatting and discards scripts and arbitrary attributes. Existing policy content does not need to be re-entered.

Buyer and Seller Account Management now include a Policies section, with a shortcut at the top of the page. Each account sees its role policy and the Prohibited Item Policy when content is available. Rows display the saved version and update date. The policy opens in a responsive dialog with a fixed heading, scrollable content and close controls.

The Logistics dashboard now links to Account Management. This new page shows the center's registered profile and the same Policies section. Its route requires authentication; the controller restricts access to approved, active logistics-center accounts. Policy access does not depend on notification history and does not record policy consent.

## Cart, checkout, notifications, and messaging

- Buyer cart now groups lines by store with a shop heading/icon, per-item and per-store checkboxes, a select-all summary, and a selected-item total. The browser remembers selected item IDs for that buyer during the session. Checkout validates selected rows server-side, creates seller-specific orders for those rows, and leaves unselected cart lines in place. Relevant files: `resources/views/buyer/cart.blade.php`, `resources/views/buyer/checkout.blade.php`, `app/Http/Controllers/Buyer/{Cart,Checkout}Controller.php`.
- Buyer checkout still notifies the seller for each order, but no longer creates an admin `new_order` notification. Admin dashboard and notification endpoints hide older `new_order` records. Admin, Buyer, and Seller bell menus place **Mark all as read** alongside **View all** with link-style hover treatment. Logistics and Courier notification modules are not implemented. Relevant files: `app/Http/Controllers/Admin/{Dashboard,Notification}Controller.php`, the three role layouts, and shared notification CSS.
- Buyer Vendo Support no longer exposes Alpine/JavaScript source in the message page; sending an empty message returns a validation response and successful asynchronous sends return JSON. Available profile photos now appear in Admin, Buyer, and Seller message threads/lists, with initial fallbacks. Buyers and sellers can end support conversations but cannot delete support or marketplace history; Admin alone has a support conversation deletion route/button. Relevant files: message controllers/views, `MessageDeletionController.php`, and `routes/web.php`.
- These UI and workflow changes have not been test-verified here; the project owner will perform manual and automated verification.

## Agent workflow and ERP review

Created root `AGENTS.md` with focused Codex rules for preserving work, reading relevant docs, documenting every code change on the Asia/Manila date, role-scoped authorization, checkout and fulfillment invariants, private files, and the owner's testing preference. Updated the docs guides and affected specs/status snapshot to match. Reviewed `/home/Snowden/Downloads/E-Com/ERP-Components-updated.pdf` and `ERP-Flow.pdf`: they define the target buyer → seller → pickup rider → sorting center → delivery rider → buyer confirmation chain. Courier and most sorting/delivery actions are still future implementation. The ERP brief names a 10% commission, while this app stores category commission settings; this is a requirement difference to resolve before changing calculation rules.

Refined `AGENTS.md` using an external agent guide as a reference. Added a map to the actual project docs, explicit current-stack boundaries, approval checks, migration and modularity guidance, scope control, and branch/commit behavior. The reference guide's `src/` applications, React/Next.js, Sanctum, PostgreSQL, central `PROGRESS.md`, and automatic branching/commits do not describe this repository; the daily docs logs and current Laravel structure remain authoritative.

## Logistics foundation

Protected the logistics dashboard and account routes with authentication, approved/active center checks, and a required center profile. Connected the existing rider approve/reject forms to center-scoped routes and limited reviews to pending courier applications, so an unrelated account or center cannot use those actions and a reviewed application cannot be reviewed again. Changed `routes/web.php`, `app/Http/Middleware/EnsureActiveLogisticsCenter.php`, and `app/Http/Controllers/Logistics/DashboardController.php`; updated the logistics specs, domain page, and feature-status snapshot. Rider app and private verification-document storage remain open. Changes have not been test-verified; the project owner will test.

Rider review decisions now lock the applicant's user row, preventing simultaneous approve/reject requests from overriding each other.

Added the first Vendo-owned logistics dispatch slice. Admin can assign a ready-for-pickup seller order to an approved center; that center can assign an approved pickup courier from its own roster. After the seller confirms handoff, the center can manually record arrival and sorting. Each assignment writes a separate audit row; status changes use the existing order timeline. Added a migration, focused admin/logistics controllers, guarded routes, and Vendo-style admin and center pages. Updated the affected admin/logistics specs, schema, architecture, order-flow decisions, domain status, and domain page. This is one center per seller order and manual routing; courier app, scans, hub/linehaul, POD, and delivery completion remain future work. No automated tests or app walkthrough were run at the owner's request.

Seller pickup now checks that the assigned courier belongs to the order's approved, active logistics center. This closes the handoff gap where an otherwise approved courier from another center could satisfy the seller action. Updated `SellerOrderWorkflow` and the seller Shipments spec. Existing unassigned or centerless ready orders need admin center routing before pickup. No tests were run; the owner will verify.

After sorting, the center can now assign a separate active delivery rider from its roster. The action stores `orders.delivery_courier_id`, transitions the order to `assigned_to_rider`, and writes status and assignment events once under a row lock. Updated the dispatch page, migration/model, routes, logistics specs, flow decisions, schema, architecture, and domain status. Rider acceptance, out-for-delivery, delivery evidence, and completion remain for the separate rider app and later integration.

Updated `docs/future-plan.md` to distinguish the new manual center operations from the remaining fulfillment work.

Seller order and shipment detail screens now label pickup and delivery riders separately and align the pickup button state with the server's center/rider checks. Updated both seller specs; the print waybill still needs a logistics ownership/label review.

## Logistics routing, hub custody, and admin accounts

Removed the previously added admin logistics dispatch and seller pickup actions, matching the owner’s actor boundaries. Seller readiness now routes to a unique approved active origin center from seller location and a unique destination center from the buyer’s structured checkout city/province. Checkout snapshots validated location codes/names; a bundled location catalog also supplies registration province/city options including Metro Manila. Ambiguous/missing center matches stay unresolved. The origin center assigns pickup and manually records arrival/sort after the future rider scan, sends cross-center parcels to the destination hub, and the destination center confirms receipt and assigns its own delivery rider. Added status events and a hub-send audit row; no physical scan, truck manifest, or delivery action is claimed. Legacy ready orders can be routed with `php artisan orders:route-ready` after applying both logistics migrations. Legacy free-text buyer addresses cannot be safely inferred. Updated checkout, order model, workflow service, dispatch controller/routes/view, migration, catalog and affected specs/schema/architecture/status docs.

Admin User Management now includes logistics-center accounts in counts, filter, list, and profile, with server-side role/status checks on suspend/deactivate/reactivate. New center approval stays in Registrations; approving or reactivating a center retries unresolved ready orders. Search/date/type navigation reloads the page so detail dialogs follow the filtered rows. Updated controller, views, and User Management spec.

Production context recorded for future deployment work: `vendo-ph.app` on Azure behind Cloudflare Tunnel. Added configurable trusted-proxy support in `config/trustedproxy.php`. An earlier `bootstrap/app.php` config lookup broke Artisan/IntelliSense before the config binding existed; removed it and used Laravel's request-time proxy config instead. No migration, automated test, or manual app walkthrough was run, per owner instruction. Deployment requires `APP_URL=https://vendo-ph.app`, `SESSION_SECURE_COOKIE=true`, the actual tunnel reverse-proxy IPs in `TRUSTED_PROXIES`, config-cache rebuild, `php artisan migrate --force`, `php artisan orders:route-ready`, and normal Laravel/Vite deployment steps on the Azure host. The owner will verify web behavior and the next phase is QR/barcode pickup scanning.

## Seller shipping label, rider scan contract, and Logistics sidebar

- Replaced the seller's plain waybill with a 4 × 6 inch print label based on the supplied screenshot. The primary wordmark is the **assigned origin logistics center business name** (for example, JNK); route hubs, addresses, COD total, Code 128 barcode and QR use the existing order data. Both codes encode only the stable Vendo tracking number. Printing requires seller ownership, pickup readiness and resolved origin/destination centers. Added Picqer barcode and Endroid QR packages and a focused code generator; changed the seller print links, label Blade/CSS, controller, and Order helper.
- Added two **unapplied** migrations for Sanctum bearer tokens and `order_scan_events`. The owner was told before the migrations were added and will run `php artisan migrate`. Added versioned rider JSON routes with approved active rider/center checks, assigned-work listing, seven-day scoped login token, logout, and idempotent pickup, origin-arrival and out-for-delivery scans. Each valid scan locks the order, writes scan/status events once, and notifies its center; seller/buyer status notifications now name the relevant logistics hub and city. The separate rider app, delivery proof/completion and linehaul scans remain future work.
- Reviewed the public Aisley Logistics sidebar as a structure reference. Added a Vendo Blade sidebar on Rider Management, dispatch sections and Account Management. Rider Management is the existing applications/approval screen; its duplicate empty route was removed after the owner's correction. Incoming Parcel Management, Parcel Sorting and Delivery assignments filter existing center-scoped dispatch actions. Delivery Monitoring, Reports and Messages have explicit placeholder pages. Matched the existing Seller workspace's plum icon rail, hover/pin expansion, header, logo and mobile drawer with Vendo assets, using the `uncodixfy` UI guidance. Protected routes, shared layout, CSS/Vite input and the logistics navigation spec were updated. No React code was copied into this Laravel app.
- Updated the relevant seller/logistics/courier specs, domain pages, status matrix, schema, architecture and order-flow decisions. Code and docs are uncommitted. No automated tests or manual app walkthrough were run, per owner instruction; static review only.

The owner reported that Print Shipping Label had disappeared. The strict first gate required both origin and destination hubs, hiding the action on ready orders whose destination was not resolved. Relaxed printing to require only readiness, tracking and an assigned origin logistics center; an unresolved destination now prints the buyer destination area with an explicit pending-hub note. The print control stays visible but disabled with a clear reason before readiness or origin assignment. Updated the seller link locations, label, model/controller gate and affected specs. Legacy ready orders still need automatic routing (`php artisan orders:route-ready`) to acquire an origin when a unique active center matches; the agent did not run the command.

## Proposed virtual inter-hub checkpoints (documentation only)

Documented the owner's Laoag origin hub → SOC5 → SOC6 → Calamba destination hub example. SOC5 and SOC6 are virtual tracking codes between actual main logistics hubs, not facilities, accounts, rider teams, or custody points. Added a dedicated proposal spec and updated the order-flow decisions, Logistics domain/status docs, shipment-status spec, future plan and docs index. Current code still records a single direct origin-to-destination hub transfer; no code or migration was changed. Before implementation, the team must define the trusted event that confirms each virtual checkpoint and the lane rules. No tests or app walkthroughs were run.

Clarified the owner's scan-by-scan intent: courier scans at seller pickup, origin hub, SOC5, and SOC6; each scan confirms the current milestone and points tracking to the next leg. Calamba main-hub arrival needs its own physical receipt confirmation after the SOC6 scan. Updated the flow decisions, virtual checkpoint spec, logistics status spec and rider API spec to distinguish this planned sequence from the three scan types currently implemented. SOC codes remain virtual route stages with no separate facility. No code, migration, tests or walkthroughs were run.
